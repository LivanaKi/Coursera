package main

import(
	"math/rand"
	"time"
	"fmt"
)
/*
func main(){
	var move rune
	var machineMove int // R is 0, P is 1, S is 2

	fmt.Println("Choose either R P or S")
	fmt.Scanf("%c", &move)
	if move == 'R'{
		fmt.Printf("my move is %c\n", move)
	}
	if move == 'P'{
		fmt.Printf("my move is %c\n", move)
	}
	if move == 'S'{
		fmt.Printf("my move is %c\n", move)
	}
	if move != 'R' && move != 'P' && move != 'S'{
		fmt.Printf("illegal move is %c\n", move)
		return
	}

	rand.Seed(time.Now().UnixNano())
	
	machineMove = rand.Intn(3)
	if machineMove == 0 && move == 'S'{
		fmt.Println("machine wins")
	}else if machineMove == 1 && move == 'R'{
		fmt.Println("machine wins")
	}else if machineMove == 2 && move == 'P'{
		fmt.Println("machine wins")
	}else{
		fmt.Println("Draw or you win")
	}*/
}